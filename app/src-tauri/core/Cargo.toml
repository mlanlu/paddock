[package]
name = "paddock-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.77"

# Deliberately no `tauri` dependency. See the workspace comment in ../Cargo.toml.
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
