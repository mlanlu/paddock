// Placeholder shell. WP-M1-3 onwards replace this with the real list, picker
// and log panel; WP-M1-1 only has to prove the window opens and builds.
const app = document.querySelector<HTMLElement>("#sandboxes");
if (app) app.textContent = "paddock";
