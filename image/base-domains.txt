# Egress allowed in every paddock image. Profiles add their own `domains`.
# GitHub (web/api/git) is added separately from https://api.github.com/meta.

# Claude Code
api.anthropic.com
sentry.io
statsig.com

# package registries
registry.npmjs.org
registry.yarnpkg.com

# GitHub release assets (postinstall binary downloads, e.g. the supabase CLI)
objects.githubusercontent.com
release-assets.githubusercontent.com
